import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginBottom: 0,
  },
  input: {
    borderRadius: 10,
    paddingTop: 10,
    fontWeight: 'normal',
  },
})

export default styles
